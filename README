Source for Andrew Davison's homepage.

To build:

    python build.py build

To view the generated pages on the local host:

    cd docs
    python -m http.server 9000 --bind 127.0.0.1

To upload, commit everything to the master branch and push to GitHub.
The site is currently served by GitHub Pages.
